#include <stdlib.h>
#include <iostream>

using namespace std;

bool isObb(int num) {
    if (num / 2 == 0) {
        return true;
    } else {
        return false;
    }
}

long get(int number, int n) {
    if (isObb(number)) {
    }
}
int main()